{
    "tasks": [
        {
            "type": "shell",
            "label": "C++ Build Project",
            "command": "make",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Build all files in src"
        }
    ],
    "version": "2.0.0"
}